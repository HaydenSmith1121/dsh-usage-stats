# 行尾一律 LF。
#
# 不是洁癖：`npm run build:check` 会逐字节比较 `lib/` 与 `src/` 的构建结果，
# 集合仓库那边还会重算 tarball 的 sha256。行尾被自动转换会让
# 「同一份源码在 Windows 与 Linux 上打出的包不是同一个字节」，那种差异极难定位。
* text=auto eol=lf

# 二进制：绝不能被当作文本处理
*.tgz binary
*.gz binary
*.png binary
*.jpg binary
*.ico binary
